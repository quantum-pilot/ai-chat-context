{
  "name": "ai-chat-context",
  "version": "1.0.0",
  "description": "Browser extension to display context window usage for AI chat interfaces",
  "scripts": {
    "build": "node build.js",
    "watch": "tsc -w",
    "clean": "rm -rf dist/*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.266",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@anthropic-ai/tokenizer": "^0.0.4",
    "esbuild": "^0.25.9",
    "tiktoken": "^1.0.22"
  }
}
